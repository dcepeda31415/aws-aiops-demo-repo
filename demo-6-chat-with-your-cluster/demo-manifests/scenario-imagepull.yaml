apiVersion: apps/v1
kind: Deployment
metadata:
  name: broken-app
  namespace: demo-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: broken-app
  template:
    metadata:
      labels:
        app: broken-app
    spec:
      containers:
      - name: broken-app
        image: mycompany/nonexistent-app:v99.99.99